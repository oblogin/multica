// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: interaction.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const assignTaskInteractionAnswers = `-- name: AssignTaskInteractionAnswers :many
UPDATE task_interaction AS i
SET status='assigned', consumed_by_task_id=target.id,
    assign_count=assign_count+1, version=version+1, updated_at=now()
FROM agent_task_queue AS target, agent_task_queue AS source, issue AS issue
WHERE target.id=$1::uuid AND target.runtime_id=$2::uuid
  AND target.dispatched_at=$3::timestamptz AND target.status='dispatched'
  AND target.issue_id IS NOT NULL AND issue.id=target.issue_id
  AND i.workspace_id=issue.workspace_id AND i.issue_id=target.issue_id
  AND i.agent_id=target.agent_id
  AND i.comment_thread_id IS NOT DISTINCT FROM target.comment_thread_id
  AND i.task_id=source.id AND source.status IN ('completed','failed')
  AND source.id<>target.id AND i.status='answered_detached'
  AND EXISTS (SELECT 1 FROM task_interaction_capability cap
    WHERE cap.task_id=target.id AND cap.runtime_id=target.runtime_id
      AND cap.claim_generation=target.dispatched_at
      AND cap.capability='task-interaction-context-v1')
  AND (i.assign_count < 2 OR target.trigger_evidence_kind IS DISTINCT FROM 'interaction_answer')
RETURNING i.id, i.task_id AS source_task_id, i.questions, i.answer,
          i.answered_by, i.answered_at, i.reason, i.assign_count
`

type AssignTaskInteractionAnswersParams struct {
	ClaimTaskID       pgtype.UUID        `json:"claim_task_id"`
	ClaimRuntimeID    pgtype.UUID        `json:"claim_runtime_id"`
	ClaimDispatchedAt pgtype.Timestamptz `json:"claim_dispatched_at"`
}

type AssignTaskInteractionAnswersRow struct {
	ID           pgtype.UUID        `json:"id"`
	SourceTaskID pgtype.UUID        `json:"source_task_id"`
	Questions    []byte             `json:"questions"`
	Answer       []byte             `json:"answer"`
	AnsweredBy   pgtype.UUID        `json:"answered_by"`
	AnsweredAt   pgtype.Timestamptz `json:"answered_at"`
	Reason       pgtype.Text        `json:"reason"`
	AssignCount  int32              `json:"assign_count"`
}

// The destination must be the exact dispatched task and claim generation
// whose response is being finalized. The source must already be terminal.
// The recipient's thread, workspace, issue and agent must all match.
func (q *Queries) AssignTaskInteractionAnswers(ctx context.Context, arg AssignTaskInteractionAnswersParams) ([]AssignTaskInteractionAnswersRow, error) {
	rows, err := q.db.Query(ctx, assignTaskInteractionAnswers, arg.ClaimTaskID, arg.ClaimRuntimeID, arg.ClaimDispatchedAt)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AssignTaskInteractionAnswersRow{}
	for rows.Next() {
		var i AssignTaskInteractionAnswersRow
		if err := rows.Scan(
			&i.ID,
			&i.SourceTaskID,
			&i.Questions,
			&i.Answer,
			&i.AnsweredBy,
			&i.AnsweredAt,
			&i.Reason,
			&i.AssignCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listWaitingInteractionTaskIDs = `-- name: ListWaitingInteractionTaskIDs :many
SELECT DISTINCT task_id FROM task_interaction
WHERE workspace_id = $1 AND task_id = ANY($2::uuid[])
  AND status = 'pending' AND expires_at > now()
`

type ListWaitingInteractionTaskIDsParams struct {
	WorkspaceID pgtype.UUID   `json:"workspace_id"`
	TaskIds     []pgtype.UUID `json:"task_ids"`
}

func (q *Queries) ListWaitingInteractionTaskIDs(ctx context.Context, arg ListWaitingInteractionTaskIDsParams) ([]pgtype.UUID, error) {
	rows, err := q.db.Query(ctx, listWaitingInteractionTaskIDs, arg.WorkspaceID, arg.TaskIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []pgtype.UUID{}
	for rows.Next() {
		var task_id pgtype.UUID
		if err := rows.Scan(&task_id); err != nil {
			return nil, err
		}
		items = append(items, task_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const recordTaskInteractionAssignment = `-- name: RecordTaskInteractionAssignment :exec
INSERT INTO task_interaction_audit
    (id, interaction_id, workspace_id, issue_id, task_id, actor_type, action)
SELECT gen_random_uuid(), i.id, i.workspace_id, i.issue_id, $1::uuid,
       'system', 'assigned'
FROM task_interaction i
WHERE i.id=$2::uuid
  AND i.consumed_by_task_id=$1::uuid
`

type RecordTaskInteractionAssignmentParams struct {
	RecipientTaskID pgtype.UUID `json:"recipient_task_id"`
	InteractionID   pgtype.UUID `json:"interaction_id"`
}

func (q *Queries) RecordTaskInteractionAssignment(ctx context.Context, arg RecordTaskInteractionAssignmentParams) error {
	_, err := q.db.Exec(ctx, recordTaskInteractionAssignment, arg.RecipientTaskID, arg.InteractionID)
	return err
}

const recordTaskInteractionContextCapability = `-- name: RecordTaskInteractionContextCapability :exec
INSERT INTO task_interaction_capability
    (task_id, workspace_id, issue_id, runtime_id, claim_generation, capability)
SELECT t.id, iss.workspace_id, t.issue_id, t.runtime_id, t.dispatched_at,
       'task-interaction-context-v1'
FROM agent_task_queue t JOIN issue iss ON iss.id=t.issue_id
WHERE t.id=$1::uuid
  AND t.runtime_id=$2::uuid
  AND t.dispatched_at=$3::timestamptz
  AND t.status='dispatched'
ON CONFLICT (task_id) DO UPDATE
SET runtime_id=EXCLUDED.runtime_id,
    claim_generation=EXCLUDED.claim_generation,
    capability=EXCLUDED.capability
`

type RecordTaskInteractionContextCapabilityParams struct {
	TaskID       pgtype.UUID        `json:"task_id"`
	RuntimeID    pgtype.UUID        `json:"runtime_id"`
	DispatchedAt pgtype.Timestamptz `json:"dispatched_at"`
}

func (q *Queries) RecordTaskInteractionContextCapability(ctx context.Context, arg RecordTaskInteractionContextCapabilityParams) error {
	_, err := q.db.Exec(ctx, recordTaskInteractionContextCapability, arg.TaskID, arg.RuntimeID, arg.DispatchedAt)
	return err
}

const settleTerminalTaskInteractions = `-- name: SettleTerminalTaskInteractions :execrows
UPDATE task_interaction AS i
SET status = CASE
      WHEN t.status = 'cancelled' THEN 'cancelled'
      WHEN i.consumed_by_task_id = t.id AND t.status = 'completed' THEN 'settled'
      WHEN i.consumed_by_task_id = t.id AND t.status = 'failed' THEN 'answered_detached'
      WHEN i.task_id = t.id AND i.status = 'pending' THEN 'open'
      WHEN i.task_id = t.id AND i.status IN ('answered','delivering','delivered') THEN 'answered_detached'
      ELSE i.status
    END,
    reason = CASE
      WHEN t.status = 'cancelled' THEN 'run_cancelled'
      WHEN i.task_id = t.id AND i.status = 'pending' AND t.status = 'failed' THEN 'process_lost'
      WHEN i.task_id = t.id AND i.status = 'pending' THEN 'run_ended'
      WHEN i.task_id = t.id AND i.status IN ('delivering','delivered') THEN 'possibly_delivered'
      ELSE i.reason
    END,
    consumed_by_task_id = CASE
      WHEN i.consumed_by_task_id = t.id AND t.status = 'failed' THEN NULL
      ELSE i.consumed_by_task_id
    END,
    version = i.version + 1,
    updated_at = now()
FROM agent_task_queue AS t
WHERE t.id = ANY($1::uuid[])
  AND t.status IN ('completed','failed','cancelled')
  AND ((i.task_id = t.id AND i.status IN ('pending','answered','delivering','delivered'))
    OR (i.consumed_by_task_id = t.id AND i.status = 'assigned')
    OR (t.status = 'cancelled' AND i.task_id = t.id AND i.status IN ('open','answered_detached')))
`

// Called in the terminal task transaction, after the task status changes.
// The task row is already locked by the status update. A later answer always
// locks that same row before the interaction row and sees the settled state.
func (q *Queries) SettleTerminalTaskInteractions(ctx context.Context, taskIds []pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, settleTerminalTaskInteractions, taskIds)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
